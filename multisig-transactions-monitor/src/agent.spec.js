const {
  FindingType,
  FindingSeverity,
  Finding,
  createTransactionEvent,
  ethers,
} = require("forta-agent");
const {
  handleTransaction,
} = require("./agent");

describe("Compound Finance multisig transaction agent", () => {});

const { Finding, FindingSeverity, FindingType } = require("forta-agent");

const handleTransaction = async (txEvent) => {
  const findings = [];

  return findings
};

module.exports = {
  handleTransaction
};
